
import 'package:flutter/material.dart';

class CountryDetail extends StatelessWidget {

  CountryDetail();
  
  @override
  Widget build(BuildContext context) {
    return Container(
        child: Center(child: Text('Country detail')),
      );
  }
}